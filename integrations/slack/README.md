# Slack integration (stub)

Planned behavior:

- Slash command: `/debrief https://github.com/org/repo`
- Bot posts the **ONEPAGER** summary in-channel with a link to the full report.

Requires a Slack app, bot token, signing secret, and server routes under `/api/integrations/slack`.
