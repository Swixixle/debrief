# Debrief — VS Code extension (stub)

Planned commands:

- **Analyze with Debrief** — context menu on a workspace folder; opens the Debrief desktop app or web UI with the path pre-filled.
- **Status bar** — “Debrief: last run …” from a lightweight API poll.

Implementation is deferred to a focused extension session. Scaffold `package.json` + `src/extension.ts` when ready.
